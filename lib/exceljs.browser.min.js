// Optimized browser bundle - minimal features for smaller size
// Excludes streaming and CSV features to reduce bundle size
// Target: 832KB → ~700KB (-15% size)

const ExcelJS = {
  // Core workbook functionality only
  Workbook: require('./doc/workbook'),
};

// Add enums
const Enums = require('./doc/enums');

Object.keys(Enums).forEach(key => {
  ExcelJS[key] = Enums[key];
});

module.exports = ExcelJS;
